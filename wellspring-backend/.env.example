# --- Database ---
MONGODB_URI=mongodb+srv://gauravk1276_db_user:G3UF5xTzxd55BPh7@cluster0.ylcpses.mongodb.net/?appName=Cluster0

# --- Auth ---
JWT_SECRET=replace-with-a-long-random-string
JWT_EXPIRES_IN=7d

# --- LLM providers for chat replies, tried in this order: gemini, grok,
# anthropic, openai. Set only the ones you actually have — see
# services/llmProvider.js. Gemini has a genuine free tier (no credit card),
# so it's a reasonable default if you're not sure which to use.
GEMINI_API_KEY=AQ.Ab8RN6Jv1-v8S8rUPx8gKZx3mZiqMIUdQFWID1OluYTlCNH-QA
XAI_API_KEY=xai-Gv2mcYK8uO5ufMUAwD6zbVScbEuikTu3uuxP4wW3hOwJW9HwjKDUEjGHp8BbI2Xt9hf0BsjIYqp9vucX


# --- Embeddings — Gemini only. xAI/Grok has no public embeddings endpoint,
# so GEMINI_API_KEY above is required regardless of which provider(s) you
# pick for chat replies. ---
EMBEDDING_MODEL=gemini-embedding-001
EMBEDDING_DIMENSIONS=3072          # gemini-embedding-001's native output size — must match scripts/createVectorIndex.js

# --- Server ---
PORT=4000
NODE_ENV=development
